export default function DeepSeekLogo({ className = 'w-5 h-5' }) {
  return (
    <div className={`${className} flex items-center justify-center rounded-md bg-blue-600 text-white font-bold text-xs`}>
      DS
    </div>
  );
}
