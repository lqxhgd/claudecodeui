export default function DoubaoLogo({ className = 'w-5 h-5' }) {
  return (
    <div className={`${className} flex items-center justify-center rounded-md bg-orange-500 text-white font-bold text-xs`}>
      豆
    </div>
  );
}
