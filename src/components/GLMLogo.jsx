export default function GLMLogo({ className = 'w-5 h-5' }) {
  return (
    <div className={`${className} flex items-center justify-center rounded-md bg-sky-500 text-white font-bold text-xs`}>
      G
    </div>
  );
}
