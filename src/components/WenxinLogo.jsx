export default function WenxinLogo({ className = 'w-5 h-5' }) {
  return (
    <div className={`${className} flex items-center justify-center rounded-md bg-blue-500 text-white font-bold text-xs`}>
      文
    </div>
  );
}
