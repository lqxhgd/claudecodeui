export default function KimiLogo({ className = 'w-5 h-5' }) {
  return (
    <div className={`${className} flex items-center justify-center rounded-md bg-purple-600 text-white font-bold text-xs`}>
      K
    </div>
  );
}
