export default function QwenLogo({ className = 'w-5 h-5' }) {
  return (
    <div className={`${className} flex items-center justify-center rounded-md bg-indigo-600 text-white font-bold text-xs`}>
      Q
    </div>
  );
}
